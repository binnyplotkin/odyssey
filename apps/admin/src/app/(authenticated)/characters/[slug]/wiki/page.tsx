import { TabStub } from "@/components/tab-stub";

export default function WikiTab() {
  return (
    <TabStub
      title="Wiki coming soon"
      description="The graph viz + page browser + detail panel lands in the next FE pass. The underlying data is already live — you can inspect it via the smoke-test script or the wiki-store directly."
    />
  );
}
