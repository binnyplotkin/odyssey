import { TabStub } from "@/components/tab-stub";

export default function IngestionTab() {
  return (
    <TabStub
      title="Ingestion coming soon"
      description="Paste a source, hit Compile, watch the LLM-driven pipeline stream live progress. The backend is ready (see @odyssey/wiki-ingest); the FE lands next."
    />
  );
}
