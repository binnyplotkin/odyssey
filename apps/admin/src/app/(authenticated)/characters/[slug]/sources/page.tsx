import { TabStub } from "@/components/tab-stub";

export default function SourcesTab() {
  return (
    <TabStub
      title="Sources coming soon"
      description="List of ingested source material with metadata, tags, provenance refs, and a per-source view of the pages it backs."
    />
  );
}
